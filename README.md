# Chrono Initialized LSTM Networks With Layer Normalization


An example is provided that demonstrates the application of the optimized LSTM architecture (CILN-LSTM) to the adding problem task. The repository will be expanded in the future with additional datasets.
